{
  "name": "surimi",
  "version": "0.1.0",
  "private": false,
  "type": "module",
  "imports": {
    "#*": [
      "./src/*",
      "./src/*.ts",
      "./src/*.tsx",
      "./src/*/index.ts"
    ]
  },
  "main": "./src/index.scss",
  "scripts": {
    "build": "vite build",
    "format": "prettier . --write",
    "prepack": "pnpm -c exec ../scripts/prepack.sh",
    "prepublishOnly": "pnpm install && pnpm build"
  },
  "devDependencies": {
    "@janis.me/linter-config": "workspace:*",
    "@janis.me/typescript-config": "workspace:*",
    "@types/node": "catalog:types",
    "sass": "catalog:build",
    "vite": "catalog:build",
    "vite-plugin-static-copy": "catalog:build"
  }
}
